- name: Build Rapier iOS (DEBUG)
  run: |
    cd common/src/main/rust/rapier

    echo "===== CURRENT DIR ====="
    pwd
    ls

    echo "===== BUILD START ====="
    cargo build --release --target aarch64-apple-ios || true

    echo "===== CHECK TARGET DIR ====="
    ls -R target || true
